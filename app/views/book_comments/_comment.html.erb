<% book_comments.each do |comment| %>
  <tr>
　<td>
　  <%= comment.comment %>
　</td>
　<td>
　  <% if comment.user == current_user %>
      <%= link_to "Destroy", book_book_comment_path(book, comment), method: :delete, remote:true, class: "btn btn-danger" %>
    <% end %>

　</td>

  </tr>
<% end %>

  <div class="comment-list bt style-2" id="comments">
    <div class="float-wrapper">
      <td></td>
      <td><h2 class="comment-count"><span id="book_comments_count"><%= book_comments.count %>件コメント</sapn></h2></td>
      <td></td>
    </div>
  </div>